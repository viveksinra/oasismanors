const successHistory = async (req,name,type,id) => {

  let userName = "Unknown"
  if(req.user.name){

  }
   

  };

const failureHistory = async (req, res, next) => {

   

  };
 
module.exports = { successHistory, failureHistory };